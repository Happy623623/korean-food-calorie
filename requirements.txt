# 딥러닝 프레임워크
torch>=2.1.0
torchvision>=0.16.0

# 1단계: 음식 영역 검출 (YOLO11). opencv/numpy/pyyaml 등 대부분의 의존성을 함께 끌어온다.
# 하이브리드 추론/학습(yolo11n 23종)에서 검증한 버전으로 고정.
ultralytics==8.4.60

# 2단계: 음식 23종 분류 백본은 torchvision EfficientNet-B0 (src/classifier/model.py).
# (timm 은 초기 프로토타입에서만 사용했고 현재 학습/추론 경로에서는 import 하지 않는다.)
timm>=0.9.0

# 데이터 증강
# 주의: albumentations 1.4.5+ 는 albucore -> stringzilla(C 확장) 를 끌어오는데,
#       Python 3.14(cp314) 용 사전빌드 wheel 이 없어 MSVC 컴파일러 없이는 설치가 실패한다.
#       albucore 의존이 없는 마지막 버전(1.4.4)으로 고정한다(사용하는 변환은 모두 호환).
albumentations==1.4.4

# 이미지 처리
Pillow>=10.0.0

# 설정 파일 파싱
pyyaml>=6.0

# 진행률 표시
tqdm>=4.66.0

# 메트릭 / 데이터 분할
scikit-learn>=1.3.0

# 시각화
matplotlib>=3.7.0
seaborn>=0.13.0

# 평가 결과 표/CSV (혼동행렬 등)
pandas>=2.0.0

# 수치 연산 (albumentations/torch 의존, 명시적으로 포함)
numpy>=1.24.0

# 3단계: 웹 데모 (app.py) — 이미지 업로드 → 분류 + 칼로리 표시
gradio>=4.0.0
